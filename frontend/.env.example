# Copy this file to .env.local and adjust for your environment.
# Vite only exposes variables prefixed with VITE_ to client code.

# Base URL of the SameShot backend API.
VITE_API_BASE_URL=http://localhost:8000
